export interface Discipline {
    id: string
    mentorId: string
    discipline: string
    created_at: string
}

export interface Tool {
    id: string
    mentorId: string
    tool: string
    created_at: string
}

export interface Skill {
    id: string
    mentorId: string
    skill: string
    created_at: string
}
